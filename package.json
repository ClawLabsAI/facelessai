{
  "name": "facelessai",
  "version": "4.39.0",
  "private": true,
  "description": "FacelessAI — single-file SPA for AI faceless-video generation (GitHub Pages).",
  "type": "module",
  "scripts": {
    "test": "node scripts/check.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "check:dist": "node scripts/check.mjs dist/index.html"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "terser": "^5.48.0"
  }
}
